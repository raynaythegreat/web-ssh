{
  "name": "web-ssh",
  "version": "2.0.0",
  "description": "Secure web-based SSH terminal with real-time WebSocket connection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "node -e \"try{require('node-pty')}catch(e){console.log('Note: node-pty not available, using fallback mode')}\""
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "socket.io": "^4.8.1",
    "uuid": "^11.0.5"
  },
  "optionalDependencies": {
    "node-pty": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
